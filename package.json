{
  "name": "extension",
  "private": true,
  "version": "1.0.0",
  "description": "package.json for task lib",
  "scripts": {
    "package": "npx tfx-cli extension create  --rev-version",
    "gallery-publish": "npx tfx-cli extension publish",
    "gallery-publish-rev": "npx tfx-cli extension publish --rev-version"
  }
}
